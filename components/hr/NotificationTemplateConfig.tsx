import React from 'react';
import PlaceholderView from '../ui/PlaceholderView';

const NotificationTemplateConfig: React.FC = () => {
    return <PlaceholderView title="Notification & Mail Configuration" />;
};

export default NotificationTemplateConfig;