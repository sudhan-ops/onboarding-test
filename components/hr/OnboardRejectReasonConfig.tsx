import React from 'react';
import PlaceholderView from '../ui/PlaceholderView';

const OnboardRejectReasonConfig: React.FC = () => {
    return <PlaceholderView title="Onboarding Rejection Reasons" />;
};

export default OnboardRejectReasonConfig;